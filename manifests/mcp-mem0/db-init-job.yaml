apiVersion: batch/v1
kind: Job
metadata:
  name: mcp-mem0-db-init
  namespace: mcp-mem0
spec:
  ttlSecondsAfterFinished: 86400
  backoffLimit: 5
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: init
          image: postgres:15-alpine
          env:
            - name: PGHOST
              value: postgresql-rw.postgresql.svc.cluster.local
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              value: postgres
            - name: PGPASSWORD
              value: postgres123
          command:
            - /bin/sh
            - -c
            - |
              set -e
              until psql -h "$PGHOST" -p "$PGPORT" -U postgres -d postgres -c '\q'; do
                echo "Waiting for Postgres..."
                sleep 2
              done
              psql -h "$PGHOST" -p "$PGPORT" -U postgres -d postgres -tAc "SELECT 1 FROM pg_database WHERE datname = 'mcp_mem0';" | grep -q 1 || \
                psql -h "$PGHOST" -p "$PGPORT" -U postgres -d postgres -c "CREATE DATABASE mcp_mem0;"
              psql -h "$PGHOST" -p "$PGPORT" -U postgres -d mcp_mem0 -c "CREATE EXTENSION IF NOT EXISTS vector;"
              echo "mcp_mem0 database and pgvector ready."
