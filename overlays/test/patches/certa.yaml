apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: certa
  namespace: argocd
spec:
  source:
    helm:
      valuesObject:
        certa:
          ingress:
            tls:
              - hosts:
                  - certa.test.net
                secretName: tls-test-net-wildcard
        domain: "test.net"
