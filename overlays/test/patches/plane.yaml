apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plane
  namespace: argocd
spec:
  sources:
  - helm:
      valuesObject:
        ingress:
          appHost: "plane.test.net"
  - targetRevision: test
