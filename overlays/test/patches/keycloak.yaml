apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: argocd
spec:
  source:
    helm:
      values: |
        ingress:
          hostname: auth.test.net
          extraTls:
          - hosts:
            - auth.test.net
            secretName: tls-test-net-wildcard
